# GPU-only cross-process event ordering — 2026-09-16

Run `11879a5f49bb2cece67bdbcf31d8c308`, candidate280/metal-127/xpcevent2,
MODE2#167, twenty-fifth exposure on boot
`2508eb6d-ddf3-497d-9774-00a7ecebe3ed`. Hardware-tested driver/QEMU unchanged.
[Artifact hashes](xpc-event-evidence-20260916.json).

Typed NSXPC transport imported MTLSharedEventHandle into a separately launched
bundle helper, using newSharedEventHandle/newSharedEventWithHandle:. First a
transport-only process pair completed with zero submissions; then one GPU round
passed; then32 rounds passed. Total33 GPU transfers/25,453,131 BGRA8 pixels,
zero mismatches. Extended process pair:parent1012/helper1013, registry4294968035,
IOSurface60,1003×769 with4096-byte rows. All three helpers acknowledged shutdown
and became absent before the parent invalidated its connection. Helpers belong to
launchd; this is not a parent waitpid/exit-status receipt.

Each consumer command buffer encodes an event wait before opening a blit encoder,
then copies the shared IOSurface to a managed readback buffer and synchronizes it.
The consumer commits before acknowledging SUBMITTED over XPC. Only then does the
producer commit buffer→surface copy followed by GPU signal. Neither process writes
the event's CPU signaledValue. Per-round seed changes and every-pixel CPU oracles
check visibility. Host completion and acknowledgments control resource reuse after
the GPU dependency, not the producer→consumer ordering under test.

Baseline CORE_PROBE_PASS; valid capture382 records/snapshot18. Guest-requested
shutdown, schema6 recovered/authorizes_launch=true, CP_STAT=0, active_after=0,
forced_inactive=0, dequeue_timeouts=0, kernel_messages=[]. PerfPowerServices0.0% CPU,
0.76s cumulative on the eleventh measured corrected-QEMU guest boot (one host boot).

Initial compile succeeded, but parent bundle signing rejected a source file at the
bundle root. The wrapper now keeps source outside the bundle; initial logs remain
archived. No GPU wait ran until signing and transport passed. Compilation has the
known global-IOSurface deprecation and unused utility warnings from building the
same source for parent/helper. The ordinary byte-archive draft was never executed.

Prelaunch attempt xpcevent reset#166 but source-pin verification rejected an unused
research header restored under src/ during dev integration. No QEMU exposure was
consumed. The test-only header was retained under its research directory, its test
include updated and synthetic checks passed. Driver src again matched its exact pin.

Scope: bounded one-direction, consumer-first GPU event synchronization across two
processes, public XPC handle transport, one managed BGRA8 IOSurface. Not concurrent
writers, render-compression hazards, sandbox entitlement coverage, arbitrary formats,
independent host boots, performance or complete Metal conformance.
