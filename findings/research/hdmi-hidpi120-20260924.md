# Correct Samsung HiDPI120 and HDMI audio — candidate330

On 2026-09-24 the user confirmed correct colors at1920×1080 logical,
3840×2160 pixels,120Hz (329), then audible Samsung headphone/audio output
with the combined330 build: "audio works ... seems perfect".
Candidate330 run `433b6f803c826755d20396e08a011a2a`, source `933d60d`, metal-178.
Display-awake assertions were verified. CoreGraphics selected and retained120Hz.

## Repairs and discriminating evidence

- 325: PowerPlay-off Navi2 capability13 incorrectly fell back to625MHz BIOS boot
clock. The native DAL getter reports1217MHz. Restoring that bounded capability
lets1188MHz timing pass the range, without forcing validation success.
- 326: DC stream validation returned6, rejecting4K120 as TMDS signal4. The sink's
FRL rate byte was zero despite checksum-valid HDMI Forum VSDB rate6.
- 327: native EDID bridge0x74ada omits caps+0x7f (sink+0x887). Restoring only the
advertised FRL rate exposes4K120. Physical output still failed; a software
preferred/verified rate field was not proof of a working display.
- 328: Linux dccg31_set_hdmistreamclk selectsDTBCLK0 and bypassesDTO for FRL.
Apple's302 path never programs315's DCCG selector. HDMISTREAMCLK_CNTL0x3519
changed0→0x10001; HPO stream FIFO error2 cleared and a physical120Hz picture
appeared, with purple tint.
- 329: HPO0 uses315 VPG5. Its infoframe SRAM was still in forced light sleep
(0x9936=0x110); the prior wake handled onlyTMDS VPG0. Extending the bounded
wake before0x9931/0x9932 packet access changed it to1. User confirmed correct colors.
-330: reportCapabilities_LinkInfo0xe074 does not recognize FRL100/400. It
published HDMI connector2048 and codec00010300 but av-signal-type0/display-typeNONE.
AppleGFXHDA asserted at framebufferEventHandler line3063. The guarded hook
publishes HDMI metadata only when the native single-link descriptor is FRL;
it preserves the transport and codec data. Samsung audio reappeared and was audible.

Native KDK24G830 prologues and callback ABIs were checked against the local Apple
corpus. Linux reference is `238650ef6`, notably dc/dccg/dcn31/dcn31_dccg.c,
dc/link/link_dpms.c, and dc/resource/dcn315/dcn315_resource.c. Native FRL training
and bandwidth validation remain enabled. No DSC or4:2:0 support was invented;
the captured EDID advertises FRL, RGB/Y444/Y422, but not DSC/Y420.

## Audio retention and log audit

At120Hz the Samsung was CoreAudio device101,2 output channels, default/system
output. A stereo tone played with exit0 and the user confirmed sound. Switching
HiDPI120→60→120 retained the same device/UID and defaults without rerouting;
playback returned0 at both rates. Final HPO FIFO error is zero; VPG5 memory is awake.
This tests those mode transitions, not every hotplug, sleep or host-boot scenario.

No guest panic or DMUB delivery timeout was found. The initial empty-ring
UNUSABLE report is followed by successful reversible readback and usable delivery.
The startup DMUB capability-query assertion and early GFXHDA4838/5107 assertions
also occurred on working323. The FRL audio3063 assertion is absent on330.
A CAIL device-ID assertion remains in startup. Repeated PSP/HDCP status4 failures
also occur on323: protected-content playback is unqualified. Do not describe the
logs as error-free or these observations as universal display/HDCP qualification.

## Final capture, shutdown and publication artifact

Candidate330 completed with valid `CORE_PROBE_PASS`, guest-request shutdown
(`exited-after-guest-request`) and recovered GPU (`authorizes_launch=true`).
Audio teardown recorded PCI command 2 and no errors: bus mastering was disabled.
The host kernel capture contains networking/firewall messages, with no GPU fault.
The VM is stopped. The host suite passed 1023 tests (3 skipped).

`kext/bin/RaphaelGPU` is the exact tested 1.0.330 executable, SHA-256
`20a1e10a123d42e89442e7e0ca096aef180e50c60c575bdbd50b7c1a4bc6235c`.
The adjacent build manifest retains its original source commit and build identity;
its build-time verification fields are not rewritten to imply later qualification.
Use the matching metal-178 configuration. No new hardware run is needed to publish
these documentation and binary-provenance updates.

[Artifact hashes](hdmi-hidpi120-evidence-20260924.json) ·
[Current setup](../../docs/hdmi-status.md).
